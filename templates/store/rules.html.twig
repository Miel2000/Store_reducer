{% extends 'base.html.twig' %}
{% block body %}
<br>
<div class="container">
    <table class="table">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Nom de la régle</th>
                <th scope="col">Rabais en %</th>

            </tr>
        </thead>
        <tbody>
        {% for item in discount_rules %}

                <tr>
                    <th scope="row">{{ item.id }}</th>

                    <td>{{ item.rule_expression }}</td>


                    <td>{{ item.discount_percent }}</td>


                </tr>
            {% endfor %}
        </tbody>
    </table>
</div>

{% endblock %}
